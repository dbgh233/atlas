# Continue Here — Atlas Pre-Call Intelligence

**Resume Point:** 2026-03-10 evening
**Phase:** 10 — Pre-Call Intelligence Refinement

## ON RESUME: Show This To User

The user was reviewing 4 data quality fixes before restarting their computer. Show this summary and ask which to implement:

---

### Data Quality Issues I Can Fix With 100% Confidence

**1. Add "onboarding" to CALL_KEYWORDS**
Jason Beeching (Certified-pep, $3M/month, $40K high ticket) and Shawn Pinske (Bayside Peptides, $500K/month) get ZERO briefs tomorrow because "onboarding" isn't a keyword. These are massive accounts getting no pre-call intelligence.
- Fix: Add "onboarding" to CALL_KEYWORDS in intelligence.py

**2. Add Hannah Ness to REP_PROFILES**
She hosts both onboarding calls but has no profile. No Slack user ID = no brief delivery.
- Fix: Add her to rep_profiles.py. Need her Slack user ID (email: hness@ahgpay.com).

**3. Fix multi-host events — brief ALL hosts**
"Meeting with Henry + Ism" (Kevin Sampson / Dad Grass) only briefs Ism (last host). Henry gets nothing.
- Fix: Loop over event_memberships, send brief per host in _process_single_call()

**4. Brian Kan LinkedIn wrong-person — ALREADY FIXED**
Name-match validation deployed. Will reject "Brian Kan - Supermicro" LinkedIn.

---

Ask: "Which of these do you want me to fix now?"

## What Was Just Deployed
- Sales vs CS brief split (Henry=sales template, Ism=CS template)
- LinkedIn name-match validation (fuzzy matching, rejects wrong-person profiles)
- Rebalanced 100-point confidence scoring
- Direct LinkedIn URLs + data source attribution footer
- Tighter prompts (merged sections, Heads Up only when non-obvious)
- Test brief posted to #sales-pipeline (Gary Trinh / Vital Peptique)

## Key Files
- `app/modules/precall/intelligence.py` — main pipeline, prompts, scoring
- `app/modules/precall/rep_profiles.py` — rep profiles + AHG context
- `app/modules/precall/router.py` — API endpoints

## Future Enhancement: Weekly Show Rate Tracking

**Goal:** Atlas automatically tracks weekly show rates per rep without anyone marking anything manually.

**Data Flow:**
1. Atlas hits Calendly API every Monday morning (scheduled job, same pattern as daily audit)
2. Pulls all events from the prior week for all reps (Henry, Ism, Hannah, Drew — all in REP_PROFILES)
3. Filters to prospect-facing event types (Discovery, Meeting, Onboarding — use CALL_KEYWORDS)
4. Checks each event's invitee `no_show` field + cancellation status
5. Posts weekly digest to #sales-pipeline: "Weekly Show Rate: Henry 95% (19/20), 1 no-show: Jeffrey Deng. 2 host-canceled without rebook: [names]"

**CLARIFICATION NEEDED (ask user on resume):**
1. Does Calendly auto-detect no-shows on AHG's plan, or does someone manually mark them?
2. If no auto-detection, we need an automated fallback. Available tools to cross-reference attendance:
   - **Otter AI** — Atlas has Otter API access (`otterai_n1ab2scHc4phFcY5MESAMLQi8iqIgsuLCtVQWyEWh9w`). If Otter recorded the meeting, it happened. No Otter transcript = likely no-show. This might be the simplest signal.
   - **Calendly no_show field** — Only reliable if plan has auto-detection or reps mark it manually
   - **Microsoft Teams API** — Azure app has Calendar permissions. Could check meeting attendance/duration. If meeting lasted <2 min or had 0 external attendees = no-show.
   - **Otter MCP** (Intelligence Hub) — can search/fetch meeting data
3. Drew's instinct: between Otter and Calendly, one of these should give us what we need without manual intervention. Audit available APIs on resume to find the most reliable automated signal.

**Architecture Decision (research on resume):**
- Check Otter API: does it expose meeting participant list or duration? If yes, "Otter has recording" = showed, "no Otter recording for scheduled Calendly event" = no-show
- Check Calendly API: what exactly does the `no_show` object contain on AHG's plan? Test with a real past event.
- Check MS Teams/Graph API: meeting attendance reports endpoint availability
- Pick the approach that requires ZERO manual intervention

**Implementation Notes:**
- New scheduler job: `weekly_show_rate` — runs Monday 8 AM EST
- New module: `app/modules/metrics/show_rate.py`
- Calendly API: `GET /scheduled_events` with date range for prior week, then check each event's invitees for `no_show` field
- Cross-reference with Otter/Teams for automated no-show detection
- Track: showed, no-showed, canceled-by-invitee, canceled-by-host, rebooked
- Store weekly snapshots in SQLite for trend tracking
- Slack format: per-rep breakdown + overall team rate + trend vs prior week
